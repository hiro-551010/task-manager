# user context — 境界・責務・依存

## 責務

- ユーザーの登録（メールアドレス + パスワード + 名前）
- ユーザー一覧・個別取得
- プロフィール更新（名前）

## 境界

- 認証セッション管理はこのコンテキストの責務外（フェーズ3以降）
- タスクとユーザーの紐付けはこのコンテキストの責務外（フェーズ3以降）

## 依存

- 他の Bounded Context への依存なし
- shared_kernel（エラー型・ID 生成）に依存する
