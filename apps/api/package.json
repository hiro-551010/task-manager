{
  "name": "@task-manager/api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "migrate:generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "migrate:generate:user": "drizzle-kit generate --config drizzle.user.config.ts",
    "migrate:user": "drizzle-kit migrate --config drizzle.user.config.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "drizzle-kit": "^0.31.9",
    "vitest": "^4.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.8",
    "postgres": "^3.4.8",
    "ulid": "^3.0.2",
    "zod": "^4.3.6"
  }
}
